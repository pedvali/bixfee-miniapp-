# 🚨 راهنمای فوری - حل مشکل آپلود Mini App

## مشکل:
صفحه https://pedvali.github.io/bixfee-miniapp-/ هنوز محتوای قدیمی را نشان می‌دهد.

## راه‌حل: آپلود فایل‌ها در Root Repository

### روش 1: آپلود دستی از طریق GitHub Web (ساده‌ترین روش)

#### مرحله 1: رفتن به Repository
1. به این آدرس بروید: https://github.com/pedvali/bixfee-miniapp-
2. روی **"Add file"** → **"Upload files"** کلیک کنید

#### مرحله 2: حذف فایل‌های قدیمی (اگر وجود دارند)
1. اگر فایل `index.html` در root وجود دارد، آن را حذف کنید
2. اگر فایل `style.css` یا `app.js` در root وجود دارد، آن‌ها را هم حذف کنید

#### مرحله 3: آپلود فایل‌های جدید
فایل‌های زیر را از پوشه `miniapp` آپلود کنید:

**⚠️ مهم:** فایل‌ها را مستقیماً در **root** repository آپلود کنید (نه در پوشه miniapp)

- `index.html` → باید در root باشد
- `styles.css` → باید در root باشد  
- `app.js` → باید در root باشد

#### مرحله 4: Commit
1. پیام Commit: `Update Mini App with Trust Wallet design`
2. **Commit changes** را بزنید

---

### روش 2: استفاده از Git (اگر Git نصب دارید)

```bash
# Clone repository
git clone https://github.com/pedvali/bixfee-miniapp-.git
cd bixfee-miniapp-

# کپی فایل‌های جدید از پوشه miniapp
cp /root/Pedram/Bixfee/my_telegram_bot/miniapp/index.html .
cp /root/Pedram/Bixfee/my_telegram_bot/miniapp/styles.css .
cp /root/Pedram/Bixfee/my_telegram_bot/miniapp/app.js .

# حذف فایل‌های قدیمی (اگر وجود دارند)
rm -f style.css  # اگر وجود دارد

# Commit و Push
git add index.html styles.css app.js
git commit -m "Update Mini App with Trust Wallet design"
git push origin main
```

---

## ⚙️ تنظیمات GitHub Pages

### مرحله 1: رفتن به Settings
1. به https://github.com/pedvali/bixfee-miniapp-/settings/pages بروید

### مرحله 2: تنظیمات Pages
**⚠️ مهم:** تنظیمات را تغییر دهید:

- **Source**: Branch = `main`
- **Folder**: `/` (root) - **نه `/miniapp`**
- **Save**

### مرحله 3: منتظر بمانید
- 2-5 دقیقه صبر کنید
- GitHub Pages را refresh کنید
- URL را تست کنید: https://pedvali.github.io/bixfee-miniapp-/

---

## ✅ چک‌لیست

- [ ] فایل‌های قدیمی از root حذف شدند
- [ ] فایل‌های جدید (`index.html`, `styles.css`, `app.js`) در root آپلود شدند
- [ ] GitHub Pages تنظیمات: Folder = `/` (root)
- [ ] 2-5 دقیقه صبر کردم
- [ ] صفحه را refresh کردم
- [ ] تست کردم: https://pedvali.github.io/bixfee-miniapp-/

---

## 🔍 بررسی ساختار Repository

بعد از آپلود، ساختار باید این‌طور باشد:

```
bixfee-miniapp-/
├── index.html      ✅ (فایل جدید)
├── styles.css      ✅ (فایل جدید)
├── app.js          ✅ (فایل جدید)
└── README.md       (اگر وجود دارد)
```

**نه این‌طور:**
```
bixfee-miniapp-/
├── index.html      ❌ (فایل قدیمی)
├── style.css       ❌ (فایل قدیمی)
└── miniapp/
    ├── index.html  ❌ (اشتباه - نباید اینجا باشد)
    ├── styles.css  ❌
    └── app.js      ❌
```

---

## 🧪 تست

1. به https://pedvali.github.io/bixfee-miniapp-/ بروید
2. باید صفحه جدید با طراحی Trust Wallet را ببینید:
   - Header با لوگو Bixfee
   - موجودی کل
   - دکمه‌های Buy/Sell/Convert
   - منوی پایین

اگر هنوز صفحه قدیمی را می‌بینید:
- Cache مرورگر را پاک کنید (Ctrl+Shift+R)
- یا در حالت Incognito تست کنید

---

## 📞 اگر مشکل حل نشد

1. بررسی کنید که فایل‌ها واقعاً آپلود شده‌اند:
   - به https://github.com/pedvali/bixfee-miniapp- بروید
   - باید `index.html`, `styles.css`, `app.js` را در root ببینید

2. بررسی تنظیمات Pages:
   - Folder باید `/` باشد (نه `/miniapp`)

3. Cache را پاک کنید:
   - مرورگر: Ctrl+Shift+R
   - یا در حالت Incognito تست کنید

