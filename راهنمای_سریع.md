# ⚡ راهنمای سریع حل مشکل "Failed to initialize app"

## 🔍 مشکل چیست؟

خطای "Failed to initialize app" به این دلیل است که **Backend API در دسترس نیست**.

## ✅ راه‌حل سریع (Demo Mode)

کد فعلی به صورت خودکار به **حالت نمایشی (Demo Mode)** می‌رود. اما برای استفاده کامل باید Backend را راه‌اندازی کنید.

---

## 🚀 راه‌اندازی Backend API

### روش 1: اجرای محلی (برای تست)

```bash
cd F:\Bixfee\my_telegram_bot\my_telegram_bot
python miniapp_api.py
```

سپس در `app.js` خط 6 را تغییر دهید:
```javascript
const API_BASE = 'http://localhost:5000/api';
```

### روش 2: آپلود روی VPS/Cloud

**گزینه 1: Railway (رایگان برای شروع)**
1. به https://railway.app بروید
2. New Project → Deploy from GitHub
3. فایل `miniapp_api.py` را Deploy کنید
4. URL دریافت کنید

**گزینه 2: Render (رایگان)**
1. به https://render.com بروید
2. New → Web Service
3. Repository را Connect کنید
4. Deploy کنید

---

## 📝 تنظیمات app.js

بعد از راه‌اندازی Backend، در `app.js` خط 5-7 را تغییر دهید:

```javascript
const API_BASE = 'https://your-backend-api.com/api';
```

**مثال:**
- Railway: `https://bixfee-api.railway.app/api`
- Render: `https://bixfee-api.onrender.com/api`
- VPS: `https://your-domain.com/api`

---

## ✅ بعد از تنظیم

1. فایل `app.js` را در GitHub آپدیت کنید
2. Mini App را تست کنید
3. باید به Backend متصل شود ✅

---

**موفق باشید! 🚀**

